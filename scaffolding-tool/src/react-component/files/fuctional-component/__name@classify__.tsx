import React from 'react'
import style from './<%= classify(name) %>.module.css'

const <%= classify(name) %> = () => {
  return (
    <div>
      
    </div>
  )
}

export default <%= classify(name) %>
